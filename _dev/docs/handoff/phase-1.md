# Phase 1 — Bootstrap · handoff

**State:** Spec complete. `_dev/docs/spec/phase-1-bootstrap.md` holds the 10 locked decisions + invariants. Sub-tasks `1.1`–`1.4` are inlined in `_dev/TODO.md`. No sub-task started yet.

**Next:** Sub-task track. Only `1.1` (Yarn 4 workspace root) is startable — `1.2`/`1.3` are parallel-eligible after it, `1.4` is last. Re-run `pick-up-task`, brief `1.1` from the spec, then choose a planning route before cutting a worktree.

**Open threads:** none — the grill closed every branch.
