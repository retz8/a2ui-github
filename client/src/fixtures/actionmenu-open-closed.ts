import {menuFixture} from './actionmenu-helpers';
import type {Fixture} from './types';

export const actionmenuOpenClosedFixture: Fixture = menuFixture('actionmenu-open-closed', {
  open: false,
});
