import {PRIMER_CATALOG_ID} from './catalog-id';

// TODO(phase-2): build the real Catalog over basicCatalog from @a2ui/react/v0_9.
// Placeholder only — no components are wired yet.
export const PRIMER_CATALOG = {id: PRIMER_CATALOG_ID, components: {}} as const;
