node_modules
.DS_Store
dist
*.tsbuildinfo
.env
.vite/
coverage/

# Python (agent/, uv-managed)
__pycache__/
*.py[cod]
.venv/
.pytest_cache/

# Playwright (client/)
test-results/
playwright-report/

# Claude Code worktrees (native EnterWorktree) + SDD scratch ledger
.claude/worktrees/
.superpowers/

# Personal, machine-local Claude instructions (auto-loaded alongside CLAUDE.md)
CLAUDE.local.md

# Yarn Modern
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/sdks
.yarn/cache
system_prompt.dump.txt
failed_stream.dump.txt
surface.dump.json
tool_shapes.dump.jsonl
# Raw per-conversation recorder output; only the finalized per-beat fixtures under
# agent/recordings/beats/ are tracked.
agent/.recordings/
